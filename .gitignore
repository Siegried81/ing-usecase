# macOS
.DS_Store

# Microsoft Office lock files
~$*

.env

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# sieg 15/09: local-only working file, not for the shared repo
CLAUDE.md

# web UI build artefacts
web/node_modules/
web/dist/

# search_interest/ (the Google Trends pipeline) patterns - *.pyc and
# __pycache__/ are already covered above.
benchmark.db
benchmark_backup_*.db
data/*.log
campaigns_streamlit_log.txt
streamlit_log.txt

# NOTE (steph 21/09): the dataset and output rules were removed at stephane's
# request, so data/raw/, data/processed/, data/fixtures/ and outputs/ are now
# trackable. data/raw holds third-party brand assets (page captures and
# screenshots) that were previously not redistributed - confirm that is still
# acceptable before pushing them.

# sieg 21/09: local AI-tool worktree/config (Kilo Code), not shared repo
# content - was causing noise and a stray tracked file in git status.
.kilo/
